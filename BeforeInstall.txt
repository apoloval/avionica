This program would install Avionica libraries & sketches in your system.

The installation process will copy the libraries & sketches into the Arduino folder of your user account. That means these libraries would be available for current user only. 
